<%= form_with(model: customerpersonalinfo, local: true) do |form| %>
  <% if customerpersonalinfo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(customerpersonalinfo.errors.count, "error") %> prohibited this customerpersonalinfo from being saved:</h2>

      <ul>
        <% customerpersonalinfo.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
