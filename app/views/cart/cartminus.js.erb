$('.cart-navbar').fadeOut(200);
$('.cart-navbar').html("<%= j Cart.where(customer_id: current_user.id).sum(:quantity) %>");
$('.cart-navbar').fadeIn(200);
$('.cart-navbar').fadeOut(200);
$('.cart-navbar').fadeIn(200);